# Copy to .env and adjust locally (never commit .env)
# Default sqlite backend used by src/train.py when the variable is unset:
# MLFLOW_TRACKING_URI=sqlite:///model/mlflow.db
MLFLOW_TRACKING_URI=sqlite:///model/mlflow.db
